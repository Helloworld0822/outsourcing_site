apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: crewlink
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 4000
      targetPort: http
    - name: ws
      port: 4001
      targetPort: ws
